<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NFC Art Platform | Presentation</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        art: {
                            bg: '#050505',
                            surface: '#0f0f10',
                            card: '#18181b',
                            accent: '#3b82f6',
                            text: '#e4e4e7',
                            muted: '#a1a1aa'
                        }
                    },
                    fontFamily: {
                        sans: ['Inter', 'system-ui', 'sans-serif'],
                        mono: ['Fira Code', 'monospace']
                    }
                }
            }
        }
    </script>
    
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    
    <!-- Three.js Import Map -->
    <script async src="https://unpkg.com/es-module-shims@1.6.3/dist/es-module-shims.js"></script>
    <script type="importmap">
        {
            "imports": {
                "three": "https://unpkg.com/three@0.160.0/build/three.module.js",
                "three/addons/": "https://unpkg.com/three@0.160.0/examples/jsm/"
            }
        }
    </script>
    
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <!-- Marked.js for Markdown rendering -->
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>

    <link rel="stylesheet" href="styles.css">
</head>
<body class="bg-art-bg text-art-text min-h-screen flex flex-col font-sans antialiased overflow-hidden selection:bg-blue-500/30">

    <!-- Header / Navigation -->
    <header class="fixed top-0 w-full z-50 bg-art-bg/80 backdrop-blur-xl border-b border-white/5">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
                <!-- Brand -->
                <div class="flex items-center gap-3">
                    <div class="bg-gradient-to-br from-blue-600 to-violet-600 p-2 rounded-lg">
                        <i data-lucide="layers" class="h-5 w-5 text-white"></i>
                    </div>
                    <div>
                        <h1 class="font-bold text-lg tracking-tight text-white leading-none">NFC Art Platform</h1>
                        <span class="text-xs text-art-muted">Final Presentation Suite</span>
                    </div>
                </div>

                <!-- Navigation Tabs -->
                <nav class="hidden md:flex space-x-1 bg-art-surface/50 p-1 rounded-xl border border-white/5">
                    <button data-tab="analytics" class="nav-tab active">
                        <i data-lucide="bar-chart-2" class="w-4 h-4"></i>
                        <span>Analytics</span>
                    </button>
                    <button data-tab="development" class="nav-tab">
                        <i data-lucide="smartphone" class="w-4 h-4"></i>
                        <span>Development</span>
                    </button>
                    <button data-tab="summary" class="nav-tab">
                        <i data-lucide="package" class="w-4 h-4"></i>
                        <span>Summary</span>
                    </button>
                    <button data-tab="innovation" class="nav-tab">
                        <i data-lucide="lightbulb" class="w-4 h-4"></i>
                        <span>Innovation</span>
                    </button>
                </nav>

                <!-- Status -->
                <div class="flex items-center gap-2 text-xs font-mono text-emerald-400 bg-emerald-500/10 px-3 py-1 rounded-full border border-emerald-500/20">
                    <span class="relative flex h-2 w-2">
                      <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>
                      <span class="relative inline-flex rounded-full h-2 w-2 bg-emerald-500"></span>
                    </span>
                    System Online
                </div>
            </div>
        </div>
        
        <!-- Mobile Nav Placeholder (Simplified for this view) -->
        <div class="md:hidden flex justify-around border-t border-white/5 bg-art-surface p-2">
            <button data-tab="analytics" class="p-2 text-art-muted hover:text-white"><i data-lucide="bar-chart-2"></i></button>
            <button data-tab="development" class="p-2 text-blue-500"><i data-lucide="smartphone"></i></button>
            <button data-tab="summary" class="p-2 text-art-muted hover:text-white"><i data-lucide="package"></i></button>
            <button data-tab="innovation" class="p-2 text-art-muted hover:text-white"><i data-lucide="lightbulb"></i></button>
        </div>
    </header>

    <!-- Main Content Container -->
    <main id="content-area" class="flex-grow pt-16 h-screen overflow-y-auto scroll-smooth custom-scrollbar relative">
        <!-- Content injected dynamically via main.js -->
    </main>

    <script type="module" src="./main.js"></script>
</body>
</html>
